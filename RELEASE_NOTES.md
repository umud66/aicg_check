# AICG Check v0.3.0

多层检测与文档结构解析升级版。

## 主要升级
- DOCX 改为按真实 XML 顺序读取段落和表格，能够识别位于表格中的摘要与实验结果。
- 正确识别并停止分析“参考文献”后的内容，避免把英文文献误归入结论章节。
- 新增摘要、研究现状、实验、结论等章节模板链检测。
- 新增章节级 Burstiness / 句长稳定性检测。
- 新增“实验结果很多但可复现过程细节不足”的证据缺口评分，不再把数字本身视为强人工证据。
- 新增文献综述/研究现状的引用缺口检测。
- 全文评分改为段落风险 + 文档级信号 + 多信号协同融合。
- 保留跨段近似重复、文风突变、词汇多样性、功能词等 v0.2 能力。
- 增加可选本地语言模型困惑度层：安装 `requirements-lm.txt` 并设置 `AICG_LM_ENABLE=1` 后启用；核心版默认不下载模型。
- Web 页面展示章节模板、低 Burstiness 章节、实验细节覆盖、引用缺口及本地 LM 状态。

## 校准说明
v0.3 使用独立的结构、风格、证据完整性信号组合评分，不针对单篇文本直接拟合某个商业检测结果。仍建议使用带标签的人工/AI/人工润色/AI润色样本持续运行 `benchmark.py` 校准阈值。

## 边界
本工具输出的是 AIGC 风险估算，不等同于知网、万方或其他闭源平台的官方检测结果，也不能单独证明文本由 AI 生成。
