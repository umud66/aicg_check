# AICG Check v0.4.0

校准与双样本回归版。

## 主要升级
- 修正章节分类：优先依据章节标题，而不是让正文里的“原理、实验、总结”等词抢走章节类型。
- 扩展方法、实验、失败案例、总结/改进思考等标题识别。
- 保留 v0.3 的 DOCX 表格解析、参考文献停止、章节模板、Burstiness、跨段相似和实验完整性分析。
- 新增“人类化实操证据”解释分，用于展示工程踩坑、具体失败、硬件/超参等细节；这些证据不再直接把整篇风险压到接近 0。
- 将复合风险分到 AIGC 风险占比的 Sigmoid 校准参数改为可配置：`AIGC_CALIBRATION_CENTER` 与 `AIGC_CALIBRATION_SCALE`。
- 默认临时校准参数为 center=34、scale=12；这是启发式校准，不是商业平台参数反推。
- Web 页面展示实操证据、校准状态与参数。
- CSV 导出增加“实操证据”字段。
- `benchmark.py` 改为评估最终 `summary.estimated_ratio`，不再错误地只评估单段原始分。

## 回归说明
本版同时使用一篇高风险生成式样本和一篇低风险/强实操细节样本做回归，目标是减少 v0.3 出现的“高风险能抓住、低风险却被压得过低”的校准偏差。测试样本只用于校准方向，不被写入程序做文件级硬编码。

## 边界
本工具输出的是 AIGC 风险估算，不等同于知网、万方或其他闭源平台的官方检测结果，也不能单独证明文本由 AI 生成。默认校准仍需更多真实人工/AI/混合文本和第三方检测结果继续验证。
